{
	"private": true,
	"scripts": {
		"clean": "rimraf packages/**/node_modules packages/**/lib",
		"watch": "lerna run watch --parallel",
		"build": "lerna run build --parallel"
	},
	"devDependencies": {
		"lerna": "^2.11.0",
		"rimraf": "^2.6.2",
		"tslint": "^5.10.0"
	},
	"workspaces": [
		"packages/*"
	]
}
